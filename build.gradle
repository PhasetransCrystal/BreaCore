allprojects {
    group = 'com.phasetranscrystal'
}

tasks.register("installGitHooks") {
    doLast {
        copy {
            from("scripts/git-hooks/pre-push")
            into(".git/hooks")
            fileMode(0b111101101) // -rwxr-xr-x
        }
    }
}